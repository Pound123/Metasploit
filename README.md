### Metasploit

- Create by pound
- Group PlusX

### How to Install
```
apt update -y && apt upgrade -y && apt install git -y && apt install figlet -y && gem install lolcat -y
```
```
cd Metasploit
```
```
chmod +x setup.sh
```
```
./setup.sh
```
